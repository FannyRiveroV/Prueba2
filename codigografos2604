matriz = [];
opcion=0;
a=1;
numero_filas=0;





def crearmatriz():
    Adyacencia=0
    for i in range(numero_filas):
        matriz.append([])
        for j in range(numero_columnas):
            print("incide el vertice[",i,"] con el","[",j,"]?")
            print("1:si      0:no")
            Adyacencia=int(input())
            if Adyacencia <=1:
                matriz[i].append(Adyacencia)
            else:
                matriz[i].append(0)
                
def imprimirMatriz():
    for i in range(numero_filas):
        for j in range(numero_filas):
                print(matriz[i][j], end=' ')
        print()



def caminos(inicio,llegada):
    print("empiezo mi ruta en",inicio);
    if matriz[inicio][llegada]==1:
        print("finalmente he llegado a",llegada);
        return 1
    else:
        if matriz[inicio + 1][llegada]==1:
            print("pasé por el vertice",inicio+1);
            return caminos(inicio+1,llegada) + 1;
        else:
            return caminos(inicio,llegada-1)




def conexo():
    inicio=0
    aux=0
    verticeFinal=numero_filas-1
    while(inicio != verticeFinal):
        for i in range(verticeFinal):
            for j in range(verticeFinal):
                if(i!=j):
                    if(matriz[i][j]!=1):     
                        aux=1

        inicio=inicio+1


                
    if(aux==0):
        print("el grafo es conexo")
    else:
        print("el grafo no es conexo")
                    





        

                    
                    
                    
                
                
while (a==1):

    print ("APP GRAFOS")
    print ("1.- Crear Grafo mediante su matriz de adyacencia")
    print ("2.- Imprimir Matriz de adyacencia")
    print ("3.- Caminos más corto para dos nodos")
    print ("4.- Verifique si es o no es conexo el grafo")
    print ("5.- ")
    print ("6.- ")
    
    print ("7.- Salir")
    opcion= int(input("////"))





    
    if opcion ==1:
        numero_filas=int(input("ingrese vertices: " ))
        numero_columnas=numero_filas
        crearmatriz()
        print()


        
    if opcion ==2:
        if numero_filas==0:
            print("ingrese una matriz primero")
            print()
        else:
            imprimirMatriz()
            print()
        
    if opcion==3:
        
        if numero_filas==0:
            print("ingrese una matriz primero")
            print()
        else:    
            inicio=int(input("inicio:"))
            llegada=int(input("llegada:"))
            print("el camino más corto entre","[",inicio,"]","[",llegada,"]","es",caminos(inicio,llegada))
            print()

    
    if opcion ==7:
        a=2;
