
matriz = [];
opcion=0;
a=1;
numero_filas=0;





def crearmatriz():
    x=0
    for i in range(numero_filas):
        matriz.append([])
        for j in range(numero_columnas):
            print("incide el vertice[",i,"] con el","[",j,"]?")
            print("1:si      0:no")
            x=int(input())
            if x <=1:
                matriz[i].append(x)
            else:
                matriz[i].append(0)
                
def imprimirMatriz():
    for i in range(numero_filas):
        for j in range(numero_filas):
                print(matriz[i][j], end=' ')
        print()
//imprime la matriz sin los [ ]


def caminos(inicio,llegada):
    if matriz[inicio][llegada]==1:
        return 1
    else:
        if matriz[inicio][llegada -1]==1:
            return caminos(llegada -1,llegada) + 1;
        else:
            return caminos(inicio,llegada-1)

//muestra los caminos de un vertice a otro ( si son 4 vertices, van del [0..3] prop. de listas...)
//aun no me muestra el camino más corto pero "funciona", lo probe con un grafo q inventé, podrian inventar mas grafos
 y ver si funciona uwu
